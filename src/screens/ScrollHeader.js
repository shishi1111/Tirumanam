import { StyleSheet, Text, View } from "react-native";
import React from "react";
import ScrollableHeader from "./animatedScrollView";

const ScrollHeader = () => {
  return (
    <View>
      <ScrollableHeader />
    </View>
  );
};

export default ScrollHeader;

const styles = StyleSheet.create({});
